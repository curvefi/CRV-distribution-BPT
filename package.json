{
  "name": "crv-distribution",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pengiundev/crv-distribution",
    "create-local": "graph create --node http://localhost:8020/ pengiundev/crv-distribution",
    "remove-local": "graph remove --node http://localhost:8020/ pengiundev/crv-distribution",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pengiundev/crv-distribution"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
